# https://docs.typo3.org/m/typo3/reference-tsconfig/13.4/en-us/UserTsconfig/html

auth {
  # BE.redirectToURL =
  # mfa.required = 1
  # mfa.disableProviders := addToList(totp)
  # mfa.recommendedProvider = totp
}
