# Classic TypoScript constants can be defined here.
#
# Normally it makes no sense to still define constants here.
# Instead, use settings.definitions.yaml to set constants of the
# sitepackage and settings.yaml to set constants of other extensions.
